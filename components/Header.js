// Header.js
import { AppBar, Toolbar, Typography } from '@mui/material';

const Header = () => {
  return (
    <AppBar position="static">
      <Toolbar>
        <Typography variant="h6">Solana Airdrop By Arpit</Typography>
      </Toolbar>
    </AppBar>
  );
};

export default Header;
